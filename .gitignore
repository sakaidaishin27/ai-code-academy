# config.js は【デモ公開中はコミットする】（中身=デモ合言葉と placeholder のみ・秘密なし）。
# 本番Supabase移行時も Publishable(anon) key は公開可。Secret/Service Role key は絶対に書かない。

# OS / エディタ
.DS_Store
node_modules/

# 本番の実データは絶対に置かない（同梱はダミーのみ）
assets/data/real-*.csv
